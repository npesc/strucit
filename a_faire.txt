ACTIONS SEMANTIQUE:

- Quand on utilise une variable / fonction, on doit verifier qu'elle a ete deja declare
- Quand on appele une fonction on doit verifier que les types des parametres sont les memes qu'a la declaration
- On doit verifier le typage pour les operation
- Avant d'ajouter dans le ts il faut verifier s'il n'existe pas deja